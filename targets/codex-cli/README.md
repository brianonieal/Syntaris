# Codex CLI Target - Tier 3 (Advisory Only)

Codex CLI consumes methodology via AGENTS.md and skills from `.codex/`. No hooks, no rule auto-application. Honor-system enforcement.

## Install

```bash
bash ../../install.sh --target codex-cli
```

## What this target installs

- `AGENTS.md` at project root
- `.codex/skills/syntaris/*` for Codex's skill system
- `foundation/*.md` templates

## Verified status: SCAFFOLD (BUILD_NEXT.md task)
