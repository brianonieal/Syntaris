# Aider Target - Tier 3 (Advisory Only)

Python-based agent. Methodology loaded via `.aider.conf.yml`'s `read:` directive.

## Install

```bash
bash ../../install.sh --target aider
```

## What this target installs

- `.aider.syntaris.md`
- `.aider.conf.yml` patched with `read:` directive
- `foundation/*.md` templates

## Verified status: SCAFFOLD (BUILD_NEXT.md task)
