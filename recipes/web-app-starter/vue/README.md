# Sub-recipe: web-app-starter/vue (STUB)

This sub-recipe is a stub. Claude Code will populate it during the BUILD_NEXT.md tasks based on the pattern established by the React sub-recipes.

Expected stack: Vue 3 + Pinia + Vite, with backend choice (Express, FastAPI, or Supabase) as a follow-up question.

When populated, this file will list the same five files (`README.md`, `CONTRACT.snippet.md`, `CODING_RULES.md`, `DEPLOYMENT.md`, `EXAMPLES.md`) as the React sub-recipes.
