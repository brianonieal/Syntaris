# EXAMPLES.md
# Real code patterns for this stack. Keep under 400 lines.
# Show the patterns Claude needs most when building.

## PATTERN 1: [name, e.g. Authenticated API endpoint]

```
[code example]
```

## PATTERN 2: [name, e.g. Database query with error handling]

```
[code example]
```

## PATTERN 3: [name, e.g. Frontend component with loading/error states]

```
[code example]
```

## ANTI-PATTERN 1: [name, e.g. Don't fetch in useEffect without cleanup]

```
[bad code]
```

Why this is wrong: [explanation]

Use this instead:

```
[good code]
```
